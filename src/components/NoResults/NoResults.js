import React from 'react';
import { Container, Header } from './NoResults.style';

const NoResults = ({ isOnload }) => {
	return (
		<Container>
		  <Header>No Results</Header>
    </Container>    
	)
};

export { NoResults };
